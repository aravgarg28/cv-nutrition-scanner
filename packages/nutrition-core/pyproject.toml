[project]
name = "snap-nutrition-core"
version = "0.0.0"
description = "Deterministic nutrition math (serving arithmetic, unit conversion, %DV). No LLM, ever."
requires-python = ">=3.12,<3.13"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/snap_nutrition_core"]
